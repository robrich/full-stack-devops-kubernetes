apiVersion: apps/v1
kind: Deployment
metadata:
  name: fullstack-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fullstack-api
  template:
    metadata:
      labels:
        app: fullstack-api
        version: IMAGE_LABEL
    spec:
      containers:
      - name: fullstack-api
        image: ACR_URL/fullstack-api:IMAGE_LABEL
        resources:
          limits:
            memory: "128Mi"
            cpu: "128m"
        ports:
        - containerPort: 5000
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:5000"
        - name: ConnectionStrings__MyDB
          value: "TODO: move to secret"
---
apiVersion: v1
kind: Service
metadata:
  name: fullstack-api
spec:
  selector:
    app: fullstack-api
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fullstack-app
spec:
  selector:
    matchLabels:
      app: fullstack-app
  template:
    metadata:
      labels:
        app: fullstack-app
        version: IMAGE_LABEL
    spec:
      containers:
      - name: fullstack-app
        image: ACR_URL/fullstack-app:IMAGE_LABEL
        resources:
          limits:
            memory: "128Mi"
            cpu: "128m"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: fullstack-app
spec:
  selector:
    app: fullstack-app
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fullstack
spec:
  rules:
  - host: fullstackdevops.AKS_URL
    http:
      paths:
      - pathType: Prefix
        path: "/api"
        backend:
          service:
            name: fullstack-api
            port:
              number: 5000
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: fullstack-app
            port:
              number: 80
